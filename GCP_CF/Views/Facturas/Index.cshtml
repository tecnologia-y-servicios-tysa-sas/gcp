@model IEnumerable<GCP_CF.Models.Factura>

@{
    ViewBag.Title = "Listado de Facturas";
    ViewBag.IsReport = false;
}

<h2 class="text-left">Facturas</h2>

@if (Model != null)
{
    <h3>Listado de Facturas</h3>

    Html.RenderPartial("Facturas/PartialFacturas", Model);
}